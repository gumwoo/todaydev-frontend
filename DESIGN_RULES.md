# Frontend Design Rules

이 문서는 AI 또는 사람이 `오늘의 개발` 프론트엔드 UI를 만들 때 지켜야 하는 디자인 기준입니다. 목표는 흔한 AI SaaS 템플릿이 아니라, 실제 서비스로 출시해도 어색하지 않은 **개발자 맞춤 기술 브리핑 리더**를 만드는 것입니다.

## 1. 디자인 방향

`오늘의 개발`은 단순 대시보드가 아니라 매일 아침 읽는 개인 기술 브리핑 서비스다.

핵심 인상:

- 개발자에게 익숙하다.
- 정보 밀도가 있지만 읽기 피로가 낮다.
- 브리핑, 뉴스레터, GitHub activity, AI 요약 노트의 중간 지점에 있다.
- 차갑고 기계적인 SaaS보다 "내가 매일 열어볼 만한 도구"처럼 느껴져야 한다.
- 화려함보다 신뢰감, 선명한 정보 구조, 좋은 읽기 경험을 우선한다.

디자인 키워드:

- editorial
- developer-native
- calm focus
- morning briefing
- source-aware
- readable density
- trustworthy AI

## 2. 참고할 서비스

직접 복제하지 말고 각 서비스의 장점만 참고한다.

- Linear: 정보 위계, 상태 표현, 조용한 고급감
- Raycast: 개발자 도구다운 속도감, 명확한 명령/액션 UI
- GitHub: 개발자에게 익숙한 metadata, repo, issue, release 표현
- Readwise Reader: 읽기 경험, 저장한 글, 요약/하이라이트 UX
- Feedly: 여러 출처를 모으는 콘텐츠 구조
- Vercel: 기술 서비스의 신뢰감, 문서형 레이아웃
- Arc Browser: 기억에 남는 제품 감성, 부드러운 인터랙션

주의:

- 레퍼런스를 외형 그대로 베끼지 않는다.
- 여러 서비스를 섞어도 화면 목적이 흐려지면 안 된다.
- 포트폴리오용 장식보다 실제 사용성을 우선한다.

## 3. 금지 패턴

AI가 자주 만드는 획일적 디자인을 방지하기 위해 아래 패턴을 금지한다.

- 보라색 그라데이션 중심의 흔한 AI SaaS 랜딩 디자인 금지
- 모든 요소를 같은 둥근 카드로 감싸는 카드 남발 금지
- 의미 없는 glassmorphism 금지
- 의미 없는 다크모드 우선 디자인 금지
- 아무 맥락 없는 3D illustration, emoji, blob 배경 금지
- 이모티콘/emoji를 장식 목적으로 사용 금지
- "좋은 아침이에요 👋", "오늘도 화이팅 🚀" 같은 감정형 템플릿 문구 금지
- 기본 system font만 사용 금지
- "Welcome back", "Get started" 같은 템플릿 문구 남발 금지
- spinner 하나만 있는 로딩 화면 금지
- 모든 출처를 똑같은 리스트 카드로 표현 금지
- AI 요약을 챗봇 말풍선처럼만 표현하는 것 금지
- 과한 그림자, 과한 radius, 과한 blur 금지
- 실제 데이터가 들어오면 깨질 것 같은 showcase UI 금지
- 화면 상단에 의미 없는 hero card, welcome card, summary card를 관성적으로 배치하는 것 금지
- "총 브리핑 수", "저장한 글 수", "관심 키워드 수"처럼 사용자의 다음 행동과 무관한 상단 통계 카드 남발 금지
- 핵심 콘텐츠보다 장식용 상단 카드가 먼저 보이는 레이아웃 금지
- 카드형 UI를 기본값으로 삼지 않는다. 카드가 필요할 때만 쓴다.

## 4. 시각 언어

색상:

- 전체 톤은 차분하고 읽기 좋은 방향으로 잡는다.
- primary color 하나에 의존하지 않는다.
- source별 색상 또는 뱃지는 의미를 가져야 한다.
- GitHub, Hacker News, DEV.to의 출처 정체성을 미묘하게 구분한다.
- 배경은 완전한 흰색/검정 단색보다 미세한 온도감이나 종이/문서 같은 느낌을 줄 수 있다.

타이포그래피:

- 기본 system font만 쓰지 않는다.
- 본문 가독성과 숫자/metadata 가독성을 분리해서 생각한다.
- heading은 서비스의 개성을 만들고, body는 오래 읽어도 편해야 한다.
- 코드, repo, source, score 등은 mono 계열을 제한적으로 활용할 수 있다.

레이아웃:

- 대시보드보다 브리핑 리더에 가깝게 구성한다.
- 첫 화면에서 "오늘 읽을 핵심"이 바로 보여야 한다.
- 왼쪽/상단에는 탐색, 중앙에는 브리핑 본문, 보조 영역에는 source/status/context를 둔다.
- 모바일에서는 읽기 흐름을 우선하고 부가 정보는 접거나 아래로 내린다.

## 5. 화면별 디자인 규칙

로그인/회원가입:

- 과한 마케팅 랜딩처럼 만들지 않는다.
- "매일 아침 개발 브리핑을 받는 도구"라는 정체성을 짧게 보여준다.
- 입력 폼은 명확하고 빠르게 완료할 수 있어야 한다.

관심사 설정:

- 키워드는 단순 badge가 아니라 사용자의 브리핑을 조율하는 재료처럼 보여야 한다.
- repo 입력은 `owner/repo` 구조를 시각적으로 이해하기 쉽게 만든다.
- 빈 상태에서는 예시 키워드와 예시 repository를 제안한다.

브리핑 홈:

- 오늘의 핵심 요약이 가장 먼저 보여야 한다.
- 브리핑 생성 버튼은 명확하지만 화면을 지배하지 않아야 한다.
- 과거 브리핑, 저장한 글, 관심사 설정으로 자연스럽게 이동할 수 있어야 한다.
- 의미 없는 welcome card나 summary stat card로 상단을 채우지 않는다.
- 첫 화면의 상단 영역은 사용자가 지금 읽거나 실행할 핵심 행동과 직접 연결되어야 한다.

브리핑 생성 중:

- spinner만 보여주지 않는다.
- GitHub, Hacker News, DEV.to, AI 요약 단계를 timeline/log처럼 보여준다.
- 진행률은 숫자보다 "지금 무엇을 하는지"가 더 중요하다.
- 실패한 source가 있어도 전체가 망한 것처럼 보이지 않게 partial 상태를 표현한다.

브리핑 상세:

- AI 한 줄 요약은 editorial note처럼 보여준다.
- 각 source별 항목은 같은 카드 복붙이 아니라 source 성격에 맞게 차이를 둔다.
- GitHub 항목은 repo/release/issue metadata가 잘 보여야 한다.
- Hacker News 항목은 score/comment/discussion 느낌이 드러나야 한다.
- DEV.to 항목은 article/tag/reading 형태가 드러나야 한다.
- 저장 버튼은 과하지 않게, 반복 사용하기 쉽게 둔다.

브리핑 히스토리:

- 단순 테이블보다 날짜별 브리핑 archive처럼 느껴지게 한다.
- 검색/필터가 없어도 시간 흐름을 이해할 수 있어야 한다.

저장한 글:

- 개인 reading list처럼 느껴져야 한다.
- memo가 있다면 본문보다 보조적으로, 그러나 찾기 쉽게 표현한다.

## 6. Example Image Direction

`example.png`의 방향성은 유지할 수 있다.

좋은 점:

- 좌측 사이드바, 중앙 브리핑 피드, 우측 보조 정보 구조가 안정적이다.
- 이모티콘 남발 없이 기능 중심 아이콘을 사용한다.
- 상단에 의미 없는 welcome card 대신 바로 브리핑 본문이 나온다.
- GitHub, Hacker News, DEV.to가 시각적으로 구분된다.
- 차분한 색과 여백으로 실제 서비스 느낌이 있다.

개선 기준:

- 우측 패널은 단순 통계보다 브리핑 맥락, 수집 상태, 필터 역할을 우선한다.
- 중앙 피드는 카드 반복보다 editorial article row 느낌을 강화한다.
- `내 브리핑 시간`, 프로필, 알림 같은 부가 요소는 실제 기능일 때만 둔다.
- 장식용 상단 카드나 감정형 문구를 추가하지 않는다.

## 7. 컴포넌트 디자인 규칙

BriefingCard:

- 제목, source, score, summary, action의 위계가 분명해야 한다.
- 모든 정보를 같은 굵기와 색으로 보여주지 않는다.
- 긴 제목과 긴 summary가 들어와도 레이아웃이 무너지지 않아야 한다.

ArticleCard:

- source별 metadata를 다르게 표현한다.
- URL은 전체를 노출하기보다 domain/source 중심으로 보여준다.
- 저장/원문 보기 action은 일관된 위치에 둔다.

ProgressBar:

- 단순 퍼센트 바보다 단계와 메시지가 함께 보여야 한다.
- partial, failed, retrying 상태를 표현할 수 있어야 한다.

SectionTabs:

- source 전환이 명확해야 한다.
- 각 source의 수집 개수와 상태를 함께 보여줄 수 있으면 좋다.

KeywordTag:

- 장식용 pill이 아니라 사용자의 관심 신호처럼 보여야 한다.
- weight나 중요도를 표현할 확장 가능성을 고려한다.

## 8. 상태 UI 규칙

모든 주요 화면은 아래 상태를 가진다.

- loading
- empty
- error
- partial success
- success
- unauthorized

규칙:

- error 화면은 사용자를 탓하지 않는다.
- partial success는 실패가 아니라 "일부 출처를 제외하고 생성됨"으로 표현한다.
- empty 상태는 다음 행동을 제안한다.
- unauthorized 상태는 조용히 로그인으로 유도한다.
- long-running 작업은 사용자가 기다릴 이유를 알 수 있게 한다.

## 9. 인터랙션과 모션

- 모션은 정보를 이해시키기 위해서만 사용한다.
- 로딩 단계 전환, 브리핑 항목 등장, source tab 전환 정도에 제한적으로 사용한다.
- 과한 bounce, spin, floating animation은 피한다.
- hover는 클릭 가능성을 알려주는 수준으로 둔다.
- 키보드 접근성이 깨지는 인터랙션은 만들지 않는다.

## 10. 접근성

- 색상만으로 상태를 구분하지 않는다.
- 텍스트 대비를 충분히 확보한다.
- 버튼과 링크의 역할을 명확히 구분한다.
- focus-visible 스타일을 유지한다.
- 아이콘만 있는 버튼에는 accessible label을 둔다.
- 모바일 터치 영역은 충분히 크게 둔다.

## 11. 실제 서비스 품질 기준

- 실제 API 데이터가 길거나 비어 있거나 일부 실패해도 화면이 버텨야 한다.
- skeleton과 empty state는 실제 화면 구조와 닮아야 한다.
- 데모용 가짜 문구가 남아 있으면 안 된다.
- 모든 화면은 데스크톱과 모바일에서 모두 읽을 수 있어야 한다.
- 새 기능을 만들 때 "이 화면을 내일 아침에도 열고 싶은가?"를 기준으로 판단한다.

## 12. AI 디자인 체크리스트

AI가 UI를 만들기 전 반드시 확인한다.

- 이 화면은 대시보드인가, 브리핑 리더인가?
- 모든 요소가 똑같은 카드로 처리되지 않았는가?
- 보라색 SaaS 템플릿 느낌으로 흐르지 않았는가?
- 장식용 이모티콘이나 감정형 문구를 넣지 않았는가?
- 의미 없는 상단 hero/welcome/stat card를 만들지 않았는가?
- source별 정보 성격이 다르게 보이는가?
- spinner 하나로 긴 작업을 때우지 않았는가?
- 실제 데이터가 길어져도 깨지지 않는가?
- empty/error/partial 상태가 있는가?
- 디자인이 예쁜 척보다 읽기 경험을 우선하는가?
- 모바일에서 읽기 흐름이 자연스러운가?
- 이 화면이 실제 서비스처럼 신뢰감을 주는가?

